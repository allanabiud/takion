abstract class AppDurations {
  static const Duration searchDebounce = Duration(milliseconds: 500);
  static const Duration seriesSearchDebounce = Duration(milliseconds: 300);
  static const Duration defaultKeepAlive = Duration(minutes: 5);
}
